

function Divisao(props) {
    return (

        <div>
            <label className="label-divisao">{props.label}</label>
            <hr className="divisao"/>
        </div>
    );
}

export default Divisao;